import React from 'react'
import SectionHead from './SectionHead'

const ContactSection = () =>
{
    return (
        <section id='contact' className='text-white h-screen' >
            <SectionHead>Contact Us</SectionHead>
        </section>
    )
}

export default ContactSection